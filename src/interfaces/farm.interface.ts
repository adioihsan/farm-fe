export interface IFarm {
  id: string;
  userId: string;
  farmName: string;
  ownerName: string;
  location: string;
  isPartner: string;
  createdAt: string;  // ISO date string
  updatedAt: string;  // ISO date string
}